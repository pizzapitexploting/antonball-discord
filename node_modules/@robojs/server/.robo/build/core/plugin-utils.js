import { pluginOptions } from "../../../.robo/build/events/_start.js";
export let _readyPromiseResolve;
export const _readyPromise = new Promise((resolve)=>{
    const interval = setInterval(()=>{
        if (globalThis.roboServer?.ready) {
            clearInterval(interval);
            _readyPromiseResolve();
        }
    }, 400);
    _readyPromiseResolve = resolve;
});
export function getServerEngine() {
    return pluginOptions.engine;
}
/**
 * Returns a promise that resolves when the plugin is all set up.
 */ export function ready() {
    return _readyPromise;
}
